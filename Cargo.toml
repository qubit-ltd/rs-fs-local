[package]
name = "qubit-fs-local"
version = "0.1.0"
edition = "2024"
rust-version = "1.94"
authors = ["Haixing Hu <starfish.hu@gmail.com>"]
license = "Apache-2.0"
repository = "https://github.com/qubit-ltd/rs-fs-local"
description = "Local filesystem provider for qubit-fs"
homepage = "https://github.com/qubit-ltd/rs-fs-local"
documentation = "https://docs.rs/qubit-fs-local"
readme = "README.md"
include = [
  "/Cargo.toml",
  "/LICENSE",
  "/README.md",
  "/README.zh_CN.md",
  "/benches/**",
  "/doc/**",
  "/src/**",
  "/tests/**",
]
keywords = ["filesystem", "local", "provider", "qubit"]
categories = ["filesystem"]
metadata.docs.rs.all-features = true

[lib]
name = "qubit_fs_local"
doctest = true

[[test]]
name = "local_file_system_provider_tests"
path = "tests/local_file_system_provider_tests.rs"
required-features = ["registry"]

[[test]]
name = "local_file_uri_path_tests"
path = "tests/local_file_uri_path_tests.rs"
required-features = ["registry"]

[[test]]
name = "local_provider_mode_tests"
path = "tests/local_provider_mode_tests.rs"
required-features = ["registry"]

[[bench]]
name = "local_filesystem_facade"
harness = false

[dependencies]
qubit-fs = { path = "../rs-fs", version = "0.2", default-features = false }
qubit-local-files = { path = "../rs-local-files", version = "0.3" }
qubit-io = { path = "../rs-io", version = "0.15" }
qubit-fs-registry = { path = "../rs-fs-registry", version = "0.1", optional = true }
qubit-spi = { path = "../rs-spi", version = "0.11", optional = true }

[features]
default = []
registry = ["dep:qubit-fs-registry", "dep:qubit-spi"]

[dev-dependencies]
criterion = "0.5"
tempfile = "3.27"
qubit-io = { path = "../rs-io", version = "0.15" }
qubit-fs-testkit = { path = "../rs-fs-testkit", version = "0.1" }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(coverage)'] }
